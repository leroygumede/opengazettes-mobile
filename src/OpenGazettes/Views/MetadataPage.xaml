<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OpenGazettes.Views.MetadataPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ScrollView>
        <StackLayout
            Padding="10">
            <StackLayout>
                <Label
                    Style="{StaticResource LabelTitle}"
                    Text="FileName" />
                <Label
                    Text="{Binding SearchResult.FileName}" />
            </StackLayout>

            <StackLayout>
                <Label
                    Style="{StaticResource LabelTitle}"
                    Text="Created At" />
                <Label
                    Text="{Binding SearchResult.CreatedAt}" />
            </StackLayout>

            <!--<Button
                Text="Download"
                Command="{Binding DownloadFileCommand}" />-->
        </StackLayout>
    </ScrollView>
</ContentPage>